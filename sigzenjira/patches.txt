[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
#
# Only three things belong here:
#   1. data migrations (backfill / transform existing rows)
#   2. deletions (fixtures only insert+update, they never remove)
#   3. docs that aren't in `fixtures` (hooks.py) - Workflow, Workspace
#
# Everything else - Custom Field, Property Setter, Custom DocPerm, Report,
# Number Card, Dashboard Chart, Dashboard, Kanban Board - is a fixture and is
# re-applied on EVERY `bench migrate` (frappe/utils/fixtures.py, force=True).
# Edit custom_fields.py / install.py, run `bench export-fixtures`, commit the
# JSON. Do not add a patch for it.
sigzenjira.patches.v0_0.normalize_custom_field_names
sigzenjira.patches.v0_0.add_additional_hours_request_workflow
sigzenjira.patches.v0_0.create_pm_dashboard
sigzenjira.patches.v0_0.backfill_task_split_assign_ecd
sigzenjira.patches.v0_0.remove_task_project_name_field
sigzenjira.patches.v0_0.wire_project_user_hour_flags
sigzenjira.patches.v0_0.widen_additional_hours_request_approval_role
sigzenjira.patches.v0_0.backfill_task_split_actual_hours
sigzenjira.patches.v0_0.add_work_board_shortcut
sigzenjira.patches.v0_0.remove_all_kanban_boards
sigzenjira.patches.v0_0.clamp_actual_extra_hours_at_zero
sigzenjira.patches.v0_0.remove_planned_billable_hours_field
sigzenjira.patches.v0_0.backfill_non_billable_hours
